name: auto-release
on:
  push:
  pull_request:

jobs:
  golangci:
    runs-on: ubuntu-latest
    steps:

      - name: auto-release
        uses: alecsammon/gh-action-auto-release@v0.1.0
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
